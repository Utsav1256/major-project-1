<header>
    Page Header

    <ul>
        <% if(locals.user) { %>
            <li>
                <a href="/users/profile">
                    <%= user.name %>
                </a>
            </li>
            <!-- local.user and user is the same thing,
            we are doing this so that, we do not have to handle the error which says
        user variable not faund,
    bcz. the property can be nill but the variable cannot be just absent, it has to be declared somewhere-->
             <li>
                 <a href="/users/sign-out">Sign Out</a>
            </li>
        <% }else { %>
            <li>
                <a href="/users/sign-in">Sign In</a>
           </li>

           <li>
            <a href="/users/sign-up">Sign Up</a>
           </li>
        <% } %>
    </ul>
</header>